import React from 'react';

const LoadingPage = () => {
    return (
        <div className="loading-indicator" style={{display: 'block', textAlign: 'center', marginTop: '100px'}}>
            Loading ...
        </div>
    );
}

export default LoadingPage;